{:paths ["src" "test"]
 :tasks
 {test {:doc "Run toritate cljc actor boundary tests."
        :task (do
                (require 'clojure.test
                         'toritate.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'toritate.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
